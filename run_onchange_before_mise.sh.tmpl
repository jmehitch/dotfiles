#!/bin/bash
# mise config hash: {{ include "dot_config/mise/config.toml" | sha256sum }}
set -e
eval "$($(which mise) activate bash)"
mise use -g python
mise x -- pip install --user pipx
mise x -- pip install --upgrade pip
mise install
